{
  "name": "@factstack/chrome-ext",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "FactStack Chrome MV3 side-panel surface. A narrow, persistent companion that analyzes the GitHub repo open in your active tab — or a local folder — fully client-side, reusing the isomorphic @factstack/core analyzer. Renders a sidebar-specific, list-first UI over the same VizArtifact the dashboard consumes.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "make-demo": "tsx scripts/make-demo.mjs",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'no lint yet' && exit 0"
  },
  "dependencies": {
    "@factstack/core": "workspace:*",
    "@factstack/emit": "workspace:*",
    "@factstack/fs-browser": "workspace:*",
    "@factstack/fs-memory": "workspace:*",
    "@factstack/spec": "workspace:*",
    "@factstack/ui-theme": "workspace:*",
    "remix": "3.0.0-beta.6"
  },
  "devDependencies": {
    "tsx": "^4.19.1",
    "typescript": "^5.6.0",
    "vite": "^8.0.8"
  }
}
