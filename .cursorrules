You are working in the **factstack** codebase — a typescript (Prettier, React, Remix, Turborepo) project.

A pnpm monorepo containing a browser extension, a CLI, an MCP server, a Remix dashboard, and a VS Code extension.

## Project facts

- **Stats**: 308 files · 75.4K LOC · 962.2K tokens
- **Languages**: typescript (47%), html (24%), markdown (13%)
- **Frameworks**: Prettier, React, Remix, Turborepo, TypeScript, Vite, Vitest, Zod
- **Capabilities**: Renders a React web UI; Organised as a Turborepo monorepo; Built with Vite; Validates data with Zod schemas; Lints with a modern ESLint flat config

## Entry points

Common dev commands + URLs the project exposes:

- `npm run dev`
- `npm run build`
- `npm run test`
- `http://localhost:3000`

## Read these first

Hub files in the import graph (read before answering structural questions):

- `packages/spec/src/index.ts` — imported by 44 other files
- `apps/ui-remix/src/lib/loadArtifacts.ts` — imported by 21 other files
- `apps/ui-remix/src/ui/Section.tsx` — imported by 17 other files
- `apps/ui-remix/src/ui/FootnoteChip.tsx` — imported by 14 other files
- `apps/ui-remix/src/ui/MarginColumn.tsx` — imported by 14 other files

## Routes

- GET `/` (node-http)
- GET `/api/events` (node-http)
- GET `/api/file` (node-http)
- GET `/api/outline` (node-http)
- POST `/api/reanalyze` (node-http)
- GET `/data/factstack.json` (node-http)
- GET `/index.html` (node-http)
- GET `/about` (remix)
- GET `/config` (remix)
- GET `/credentials` (remix)
- GET `/files` (remix)
- GET `/flow` (remix)

## Security

This project has **2** known dependency vulnerabilities. When suggesting dep upgrades, prefer the versions that resolve them. Run `factstack scan-vulns` to refresh the list.

## Workflow conventions

- Match existing file structure + naming conventions when adding code.
- Prefer extending existing modules over creating new ones unless the seam is justified by 2+ adapters.
- Re-read this rules file via `cat .cursorrules` if you're uncertain about project context.
- If the FACTS MCP server is connected, prefer its tools (`read_memory`, `query_graph`, `list_risks`, `list_vulnerabilities`) over re-walking the filesystem.

---

<!-- Auto-generated by FACTS · factstack 0.1.0 · 2026-05-26T05:34:24.090Z -->
<!-- Refresh: `factstack export-skills --target cursor` -->
