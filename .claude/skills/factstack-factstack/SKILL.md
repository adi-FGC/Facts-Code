---
name: factstack-factstack
description: "Project context for factstack (typescript with Prettier/React/Remix) — A pnpm monorepo containing a browser extension, a CLI, an MCP server, a Remix dashboard, and a VS Code extension.. Use when answering questions about this codebase; reads from the FACTS MCP server for live data."
version: 0.1.0
user-invocable: true
---

## Mandatory preparation

Before answering any question about this project, call these MCP tools in order:

1. `read_memory`
2. `analyze`
3. `query_graph`
4. `list_risks`
5. `list_credentials`
6. `list_vulnerabilities`

If the FACTS MCP server is not connected, the rest of this skill is a stale snapshot — verify any factual claim against current source.

## At a glance

- **Project**: factstack
- **Intent**: A pnpm monorepo containing a browser extension, a CLI, an MCP server, a Remix dashboard, and a VS Code extension.
- **Languages**: typescript (47%), html (24%), markdown (13%)
- **Frameworks**: Prettier, React, Remix, Turborepo, TypeScript, Vite, Vitest, Zod
- **Stats**: 308 files · 75.4K LOC · 962.2K tokens

## Capabilities

- Renders a React web UI
- Organised as a Turborepo monorepo
- Built with Vite
- Validates data with Zod schemas
- Lints with a modern ESLint flat config

## Entry points

- `npm run dev`
- `npm run build`
- `npm run test`
- `http://localhost:3000`

## Key files

Most-imported files — read these first to understand the project surface:

- `packages/spec/src/index.ts` (imported by 44)
- `apps/ui-remix/src/lib/loadArtifacts.ts` (imported by 21)
- `apps/ui-remix/src/ui/Section.tsx` (imported by 17)
- `apps/ui-remix/src/ui/FootnoteChip.tsx` (imported by 14)
- `apps/ui-remix/src/ui/MarginColumn.tsx` (imported by 14)

## Routes

**node-http**
- GET `/`
- GET `/api/events`
- GET `/api/file`
- GET `/api/outline`
- POST `/api/reanalyze`
- GET `/data/factstack.json`
- GET `/index.html`

**remix**
- GET `/about`
- GET `/config`
- GET `/credentials`
- GET `/files`
- GET `/flow`


## Known vulnerabilities

2 CVE/GHSA advisory match dependencies in this project. Call `list_vulnerabilities` for details (id, severity, fixed version, advisory URL).

---

<!-- Auto-generated by FACTS · do not edit. Run `factstack export-skills` to refresh. -->
<!-- factstack 0.1.0 · generated 2026-05-26T05:34:24.090Z -->
