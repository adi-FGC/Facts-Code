{
  "name": "@factstack/intent",
  "version": "0.0.0",
  "private": true,
  "description": "Deterministic intent generator. Composes signals from agent + human artifacts (frameworks, monorepo shape, sub-apps, routes, capabilities) into a single CXO-readable sentence describing what a project IS. No LLM; no Date; no I/O — pure isomorphic function.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "test:cov": "vitest run --coverage",
    "lint": "echo 'no lint yet' && exit 0",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@factstack/spec": "workspace:*"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.8",
    "typescript": "^5.6.0",
    "vitest": "^4.1.8"
  }
}
