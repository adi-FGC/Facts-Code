{
  "name": "@factstack/graph",
  "version": "0.0.0",
  "private": true,
  "description": "Dependency graph + outline graph builders. Isomorphic. Builds from extractor output into the canonical graph shape in @factstack/spec.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "test:cov": "vitest run --coverage",
    "lint": "echo 'no lint yet' && exit 0",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@factstack/extractors": "workspace:*",
    "@factstack/spec": "workspace:*"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.8",
    "typescript": "^5.6.0",
    "vitest": "^4.1.8"
  }
}
