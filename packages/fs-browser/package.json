{
  "name": "@factstack/fs-browser",
  "version": "0.0.0",
  "license": "UNLICENSED",
  "private": true,
  "description": "Browser-side FactsFS implementations: FsaBrowserFS over the File System Access API for local-directory scans, and fetchGitHubToMemory() for fetching public GitHub repos via the Trees API + raw.githubusercontent.com into a MemoryFS the analyzer can consume.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./github": "./src/github.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "lint": "echo 'no lint yet' && exit 0",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@factstack/fs-memory": "workspace:*",
    "@factstack/spec": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^4.1.11"
  }
}
