{
  "name": "@factstack/parsers",
  "version": "0.0.0",
  "private": true,
  "description": "web-tree-sitter WASM grammar registry. Isomorphic — runs in Node and the browser (constraint C1). No native bindings.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "echo 'no tests yet' && exit 0",
    "lint": "echo 'no lint yet' && exit 0",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@factstack/spec": "workspace:*",
    "web-tree-sitter": "^0.24.3"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
