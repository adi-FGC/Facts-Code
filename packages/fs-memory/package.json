{
  "name": "@factstack/fs-memory",
  "version": "0.0.0",
  "private": true,
  "description": "In-memory FactsFS for tests and fixture-driven CI.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "echo 'no tests yet' && exit 0",
    "lint": "echo 'no lint yet' && exit 0",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@factstack/spec": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
