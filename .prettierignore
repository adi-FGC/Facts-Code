# Prettier formats Markdown in this repo; oxfmt owns everything else
# (.oxfmtrc.json ignores **/*.md so the two never fight over a file).

# Not ours / not authored here
node_modules/
legacy/
bench/corpus/
**/dist/
.facts/
.claude/
prototype/

# Generated by FACTS renderers (packages/skills/src/renderers/*): reformatting
# them only flips the bytes back on the next `factstack export-skills`.
AGENTS.md
.github/copilot-instructions.md

# Sent verbatim to a model as prompt text; keep the bytes stable.
evaluator/workflows/

# Machine-parsed release metadata (changesets owns the frontmatter).
.changeset/
