{
  "name": "@factstack/prototype",
  "version": "0.0.0",
  "private": true,
  "description": "Standalone HTML prototype + the inline Node scanner (scripts/scan.mjs). Not a runtime surface — just the design playground that the editorial v0.1 UI was iterated on.",
  "type": "module",
  "scripts": {
    "scan": "node scripts/scan.mjs \"..\" > data/factstack.json"
  },
  "dependencies": {
    "@babel/parser": "^7.25.6"
  }
}
