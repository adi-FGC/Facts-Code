diff --git a/dist/runtime/component.js b/dist/runtime/component.js
index a8ada75d000be4b6f5cd39deca0f25d560b0240e..034e3b914f2b889325914c7f4c31bd202680e3da 100644
--- a/dist/runtime/component.js
+++ b/dist/runtime/component.js
@@ -18,8 +18,15 @@ class ComponentRuntime {
     #renderController;
     #renderFn;
     #removed = false;
+    // Default queues the request until setScheduleUpdate wires the real
+    // scheduler (called by renderComponent AFTER the setup fn returns).
+    // The original threw "scheduleUpdate not implemented", which surfaced
+    // as an unhandled rejection whenever a component called handle.update()
+    // synchronously from its setup body. With the queued default, the
+    // request is replayed once setScheduleUpdate is installed.
+    #pendingScheduleUpdate = false;
     #scheduleUpdate = () => {
-        throw new Error('scheduleUpdate not implemented');
+        this.#pendingScheduleUpdate = true;
     };
     #tasks = [];
     constructor(config) {
@@ -56,6 +63,10 @@ class ComponentRuntime {
     };
     setScheduleUpdate = (nextScheduleUpdate) => {
         this.#scheduleUpdate = nextScheduleUpdate;
+        if (this.#pendingScheduleUpdate) {
+            this.#pendingScheduleUpdate = false;
+            nextScheduleUpdate();
+        }
     };
     getContextValue = () => this.#contextValue;
     isRemoved = () => this.#removed;
diff --git a/src/runtime/component.ts b/src/runtime/component.ts
index 6531048623290b05bfd08a4441aa7524b07da381..e5880db74050ce4517950a9d767f102d3039d0f9 100644
--- a/src/runtime/component.ts
+++ b/src/runtime/component.ts
@@ -253,8 +253,15 @@ class ComponentRuntime<C = NoContext> implements ComponentHandle<C> {
   #renderController: AbortController | undefined
   #renderFn: RenderFn | undefined
   #removed = false
+  // Default queues the request until setScheduleUpdate wires the real
+  // scheduler (called by renderComponent AFTER the setup fn returns).
+  // The original threw "scheduleUpdate not implemented", which surfaced
+  // as an unhandled rejection whenever a component called handle.update()
+  // synchronously from its setup body. With the queued default, the
+  // request is replayed once setScheduleUpdate is installed.
+  #pendingScheduleUpdate = false
   #scheduleUpdate: () => void = () => {
-    throw new Error('scheduleUpdate not implemented')
+    this.#pendingScheduleUpdate = true
   }
   #tasks: Task[] = []
 
@@ -300,6 +307,10 @@ class ComponentRuntime<C = NoContext> implements ComponentHandle<C> {
 
   setScheduleUpdate = (nextScheduleUpdate: () => void): void => {
     this.#scheduleUpdate = nextScheduleUpdate
+    if (this.#pendingScheduleUpdate) {
+      this.#pendingScheduleUpdate = false
+      nextScheduleUpdate()
+    }
   }
 
   getContextValue = (): C | undefined => this.#contextValue
